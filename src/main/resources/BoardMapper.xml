<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.work.board.board.mapper.BoardMapper" >
    <select id="select" parameterType="String" resultType="com.work.board.board.dto.BoardDTO">
        select * from table명 (필요한 쿼리문 작성) - 출력
    </select>
    <insert id="Mapper파일의 선언된 메소드명">
        INSERT INTO BOARD (HIT, TITLE, WRITEDATE, WRITER) VALUES (#{hit}, #{title}, #{writeDate}, #{writer})  (필요한 쿼리문 작성) -입력
    </insert>
</mapper>